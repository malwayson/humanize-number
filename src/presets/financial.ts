import { HumanizeOptions } from "../types";

export const financial: HumanizeOptions = {
  precision: 2,
  delimiter: ",",
  separator: ".",
  spacer: "",
  round: false,
};

export default financial;
